const s = "tree";
// "eert"

var frequencySort = function(s) {
    const freq = Array(s).fill(0);
    for(let i=0; i<s.length; i++) {
        for(let j=i+1; j<s.length; j++) {
            // if(s[i] !== s[j])
        }
    }
    // console.log(freq)
};

console.log(frequencySort(s))